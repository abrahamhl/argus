import { Observation } from '@argus/schema';
export interface HttpCollectorOptions {
    validateRedirect?: (url: string) => Promise<boolean>;
    maxRedirects?: number;
    timeoutMs?: number;
}
export interface HttpCollectionResult {
    observations: Observation[];
    finalUrl: string;
    redirectChain: string[];
}
export declare function collectHttp(url: string, runId: string, targetId: string, options?: HttpCollectorOptions): Promise<HttpCollectionResult>;
//# sourceMappingURL=http.d.ts.map